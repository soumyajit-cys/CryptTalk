# CryptTalk
CryptTalk is a secure chat application implementing End-to-End Encryption (E2EE) to ensure private communication. Messages are encrypted on the client using ECDH key exchange and AES-256-GCM, with replay and tamper protection. The server only relays encrypted data and never accesses plain text.
